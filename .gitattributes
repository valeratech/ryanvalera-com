# Normalize text files to LF in the index and on checkout.
# text=auto lets git detect text vs binary rather than forcing it; eol=lf
# overrides core.autocrlf so a fresh checkout does not produce CRLF bytes.
* text=auto eol=lf
